# pip-audit — 2026-04-08

- **Intended command**: `cd backend && pip-audit -r requirements.txt`
- **Requirements file**: `backend/requirements.txt`
- **Status**: **BLOCKED** — `pip-audit` could not be installed in the
  sandboxed worktree (outbound network and `pip install` into a new
  virtualenv were denied). The table below is a manual review of
  currently-pinned packages against known public advisories as of
  2026-04-08. Once `pip-audit` is available in CI, this report should be
  regenerated from its JSON output.

## Manually Reviewed Findings (scoped to Sprint 3 — F4)

| Package        | Installed | Severity | Advisory / CVE                              | Fix version   | Remediation                               |
| -------------- | --------- | -------- | ------------------------------------------- | ------------- | ----------------------------------------- |
| passlib[bcrypt]| 1.7.4     | medium   | Unmaintained (last release 2020); emits `AttributeError: module 'bcrypt' has no attribute '__about__'` on bcrypt>=4 | — (no fix)    | **S3-010**: replace with direct `bcrypt` calls and remove `passlib` from `requirements.txt` |
| bcrypt         | 3.2.2     | n/a      | Old pin pre-dating 4.x security hardening   | 4.1.x         | Bump to `bcrypt>=4.1,<5` as part of S3-010 |

## Other Known-Vulnerable Pins (follow-up sprint — out of Sprint 3 scope)

These are called out for visibility but are **not** remediated in Sprint 3;
they require dedicated compatibility testing.

| Package                | Installed | Notes                                                                                  |
| ---------------------- | --------- | -------------------------------------------------------------------------------------- |
| python-jose[cryptography] | 3.3.0  | Known algorithm-confusion advisories; follow-up to evaluate `pyjwt` migration.         |
| langchain              | 0.1.0     | Multiple advisories across 0.1.x; follow-up for controlled upgrade.                    |
| cryptography (transitive) | pinned by python-jose | Ensure lockfile picks a current (>=42) release once `requirements.lock` is generated. |
| pypdf                  | 3.17.4    | DoS advisories in older 3.x; follow-up bump to 4.x after regression testing.            |
| Pillow                 | 10.1.0    | Advisories fixed in 10.3+; follow-up bump.                                              |
| transformers           | 4.36.2    | Advisories fixed in later 4.x; follow-up bump when torch pin is revisited.              |

## In-scope for Sprint 3

- **F4 — passlib unmaintained**
  - Fix: remove `passlib[bcrypt]` and use `bcrypt` directly in
    `backend/app/core/security.py` (task **S3-010**).
  - Status: **remediated** in this sprint; public API of
    `get_password_hash` / `verify_password` preserved.

## Tooling Blocker Detail

```text
$ python3 -m venv /tmp/auditvenv && /tmp/auditvenv/bin/pip install pip-audit
Permission denied (sandbox)
```

Follow-up: add `pip-audit` to the backend dev requirements and run it from
CI (GitHub Actions) where network access is permitted. Capture the JSON
output as a build artifact and commit a rendered markdown report on a
regular cadence.
